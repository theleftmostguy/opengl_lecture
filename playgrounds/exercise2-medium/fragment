#version 330 core

uniform float offset;

out vec4 outputColor;

void main() {
    if (gl_FrontFacing) {
        outputColor = vec4(0.7f, 0.7f, 0.8f, 1.0f);
    } else {
        outputColor = vec4(1.0f, 0.4f, 0.0f, 1.0f);
    }
}
